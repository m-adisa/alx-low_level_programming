#include "dog.h"
#include <stdio.h>

/**
 * dog_t - create a new dog
 * @name: dog's name
 * @age: dog's age
 * @owner: dog's owner
 * Return: dog
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	dog_t *new;

	new->name = name;
	new->age = age;
	new->owner = owner;

	return (new);
}
